name: Spring Boot CI

on:
  push:
    branches:
      - main
      - staging
  pull_request:
    branches:
      - main
      - staging

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21
          cache: gradle

      - name: Grant execute permission
        run: chmod +x gradlew

      - name: Setup Chrome for Selenium
        uses: browser-actions/setup-chrome@v1

      - name: Run tests
        run: ./gradlew test --no-daemon

      - name: Build application
        run: ./gradlew clean build --no-daemon

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: yomu-jar
          path: build/libs/*.jar

  # Railway auto-deploys from the connected GitHub repo — no deploy job needed here.
  # To set up Railway: connect repo at railway.com, configure env vars in dashboard.
